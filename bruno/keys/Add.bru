meta {
  name: Add
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/key/add
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}
